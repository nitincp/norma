services:
  app-runtime:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    env_file:
      - path: ../.env
        required: false
    volumes:
      - ..:/workspace:cached
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      # These override .env so service hostnames stay correct inside the container
      - LANGFUSE_HOST=http://langfuse-web:3000
      - LITELLM_BASE_URL=http://litellm-gateway:4000
      - OLLAMA_BASE_URL=http://host-gateway:11434
    extra_hosts:
      - "host-gateway:host-gateway"
    networks:
      - norma-net
    command: sleep infinity

networks:
  norma-net:
    external: true
    name: norma-net
